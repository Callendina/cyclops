apiVersion: 1

datasources:
  - name: Loki
    uid: cyclops-loki
    type: loki
    access: proxy
    url: http://loki:3100
    isDefault: true
    editable: true
    jsonData:
      maxLines: 5000
      timeout: 60
