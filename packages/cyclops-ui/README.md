# cyclops-ui

Branded Flask wrapper around Grafana for the Cyclops observability stack. Sits behind Caddy + Gatekeeper. Provides a landing page, an app picker, and iframe-embedded dashboards.

## Status

Placeholder. Real implementation arrives in Phase 4. See [`../../DESIGN.md`](../../DESIGN.md) §10 for the v1 spec.

## Versioning

Released independently of the `cyclops` library via `ui-vX.Y.Z` tags in the parent monorepo.
